{
  "name": "dnd5e-divinitypoints",
  "version": "2.5.1",
  "private": true,
  "description": "Dev tooling for the Divinity Points FoundryVTT module",
  "scripts": {
    "check": "tsc --noEmit -p jsconfig.json",
    "watch": "tsc --noEmit --watch -p jsconfig.json",
    "createSymlinks": "node ./tools/create-symlink.mjs",
    "postinstall": "npm run createSymlinks"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  }
}
